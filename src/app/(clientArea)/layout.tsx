/** Minimal layout for the client-facing fit experience — no sidebar, no pro chrome. */
export default function ClientAreaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
